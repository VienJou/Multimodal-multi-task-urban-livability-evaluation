# checkpoint-24000_4m_400length

Checkpoint config is tracked in this repo. The model weights file `pytorch_model.bin` (~669MB) exceeds GitHub's 100MB limit. To use this checkpoint fully:

- **Option 1**: Use Git LFS: `git lfs pull` (if LFS was set up for this file).
- **Option 2**: Copy `pytorch_model.bin` from your local training output at  
  `Livability_evaluation_baseline/livability_4M_6aspects/checkpoint-24000_4m_400length/`.
